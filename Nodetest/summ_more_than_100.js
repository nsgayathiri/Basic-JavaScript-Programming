const prompt = require('prompt-sync')();
var num = Number(prompt('Enter the Number:'));

var sum = num;

for(var sum = 0 ; sum <= 100 ; sum = sum + num1)
{ 
  var num1 = Number(prompt('Enter the Another Number:'));
}

console.log(sum);