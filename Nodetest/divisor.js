const prompt = require('prompt-sync')();
var num = Number(prompt('Enter the number:'));

for(var count=1 ; count<=num9 ; count=count+1)
 {
  if(num%count==0)
    {
       console.log(count);
    }
 }
   
   
